<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Login • MyGreenHome</title>
    <link rel="stylesheet" href="/styles/auth.css">
</head>
<body>
    <div class="auth-container">
        <h2>Login to Your Account</h2>
        
        <% if (error) { %>
            <p class="error-msg" style="text-align: center;"><%= error %></p>
        <% } %>
        <% if (message) { %>
            <p class="message-success" style="text-align: center; color: green;"><%= message %></p>
        <% } %>

        <form id="loginForm" action="/auth/login" method="POST">
            <div class="form-group">
                <label for="username">Email</label>
                <input id="username" name="username" type="email" required>
            </div>
            <div class="form-group password-group">
                <label for="password">Password</label>
                <input id="password" name="password" type="password" required>
                <button type="button" id="togglePass" class="toggle-pass">Show</button>
                <div id="capsAlert" hidden style="color:red; font-size: 12px; margin-top: 5px;">Caps Lock is ON</div>
            </div>

            <div class="form-actions">
                <button type="submit">Login</button>
                <a href="/auth/signup" class="link">Sign Up</a>
            </div>
            <p style="text-align: center; margin-top: 20px; font-size: 14px;">
                <a href="/auth/forgot-password" style="color: #4A90E2; text-decoration: none;">Forgot Password?</a>
            </p>
        </form>
    </div>
    <script src="/scripts/auth.js"></script>
</body>
</html>