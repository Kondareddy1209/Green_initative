<!DOCTYPE html>
<html>

<head>
   
  <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Essential for responsiveness -->
    <title>Home • MyGreenHome</title>
   
  <link rel="stylesheet" href="/styles/dashboard.css">
   
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
    <style>
    /* All home-specific styles are now handled in dashboard.css for better organization and responsiveness. */
    /* This style block is intentionally left empty. */
    </style>
</head>

<body>
    <%- include('partials/navbar') %>

      <div class="home-container">
          <div class="welcome-section">
              <h1>👋 Welcome, <%= user.firstName || user.username %>!</h1>
              <p>Take control of your electricity consumption and contribute to a greener future.</p>
              <div class="cta-buttons">
                  <a href="/dashboard" class="btn">Analyze Your Bill</a>
                  <a href="/dashboard/tracking" class="btn" style="background-color: #6c757d;">View Your Progress</a>
                </div>
            </div>

          <div class="analysis-summary-section">
              <h3>📊 Your Latest Analysis</h3>
              <% if (summary && summary.totalConsumption !==undefined && summary.totalConsumption !==0) { %>
                  <div class="summary-grid">
                      <div class="summary-item">
                          <strong>Total Consumption</strong>
                          <p>
                <%= summary.totalConsumption %> kWh
              </p>
                        </div>
                      <div class="summary-item">
                          <strong>CO₂ Emissions</strong>
                          <p>
                <%= summary.carbonKg %> kg/month
              </p>
                        </div>
                      <div class="summary-item">
                          <strong>Bill Amount</strong>
                          <p>₹<%= summary.bill.totalAmount.toFixed(2) %>
              </p>
                        </div>
                    </div>
                  <div class="summary-item" style="margin-top: 20px;">
                      <strong>Top Tip:</strong>
                      <p style="font-size: 1em;">
              <%= summary.savingsTip %>
            </p>
                    </div>
                <% } else { %>
                    <div class="no-data-message">
                        No bill analysis data yet. Upload your first bill to see your summary here!
                      </div>
                  <% } %>
                  </div>

          <% if (tips && tips.length> 0) { %>
              <div class="tips-section">
                  <h4>💡 Power-Saving Tips:</h4>
                  <ul>
                      <% tips.forEach(tip=> { %>
                          <li>
                <%= tip %>
              </li>
                        <% }) %>
                        </ul>
                </div>
            <% } %>

            </div>

      <%- include('partials/footer') %>
</body>

</html>
