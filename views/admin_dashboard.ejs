<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard • MyGreenHome</title>
    <link rel="stylesheet" href="/styles/dashboard.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
    <style>
        body {
            background-color: #eef2f7;
            font-family: Arial, sans-serif;
        }
        .admin-container {
            max-width: 1000px;
            margin: 30px auto;
            padding: 20px;
            background: #ffffff;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }
        .section-header {
            color: #2c3e50;
            margin-bottom: 30px;
            border-bottom: 1px solid #eee;
            padding-bottom: 15px;
            text-align: center;
        }
        .admin-stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }
        .stat-card {
            background-color: #f8f8f8;
            border: 1px solid #eee;
            border-radius: 10px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
        }
        .stat-card h4 {
            color: #555;
            margin-bottom: 10px;
            font-size: 1.1em;
        }
        .stat-card p {
            font-size: 2em;
            font-weight: bold;
            color: #2ecc71; /* Green for positive stats */
        }
        .admin-actions {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
            margin-top: 30px;
        }
        .admin-actions .action-btn {
            background-color: #007bff;
            color: white;
            padding: 12px 25px;
            border-radius: 5px;
            text-decoration: none;
            font-size: 1.1em;
            transition: background-color 0.3s ease;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .admin-actions .action-btn:hover {
            background-color: #0056b3;
        }
        .recent-signups-section h3 {
            color: #2c3e50;
            margin-bottom: 20px;
            text-align: center;
        }
        .recent-signups-list {
            list-style: none;
            padding: 0;
        }
        .recent-signups-list li {
            background-color: #fcfcfc;
            border: 1px solid #eee;
            padding: 10px 15px;
            margin-bottom: 10px;
            border-radius: 8px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .recent-signups-list li span {
            font-weight: bold;
            color: #444;
        }
        .recent-signups-list li small {
            color: #777;
            font-size: 0.9em;
        }
        .no-data {
            text-align: center;
            padding: 30px;
            font-style: italic;
            color: #777;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="admin-container">
        <h2 class="section-header">👑 Admin Dashboard</h2>

        <div class="admin-stats-grid">
            <div class="stat-card">
                <h4>Total Users</h4>
                <p><%= totalUsers %></p>
            </div>
            <div class="stat-card">
                <h4>Admin Accounts</h4>
                <p><%= adminUsers %></p>
            </div>
            <div class="stat-card">
                <h4>Total Bills Analyzed</h4>
                <p>N/A</p> </div>
        </div>

        <div class="admin-actions">
            <a href="/admin/users" class="action-btn">
                <i class="fas fa-users-cog"></i> Manage Users
            </a>
            <a href="/dashboard/home" class="action-btn" style="background-color: #6c757d;">
                <i class="fas fa-arrow-left"></i> Back to User Dashboard
            </a>
        </div>

        <h3 class="section-header" style="margin-top: 40px;">Recent Signups</h3>
        <% if (recentSignups && recentSignups.length > 0) { %>
            <ul class="recent-signups-list">
                <% recentSignups.forEach(signup => { %>
                    <li>
                        <span><%= signup.email %> (<%= signup.firstName || '' %> <%= signup.lastName || '' %>)</span>
                        <small>Joined: <%= new Date(signup.createdAt).toLocaleDateString() %></small>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <div class="no-data">No recent signups.</div>
        <% } %>
    </div>

    <%- include('partials/footer') %>
</body>
</html>