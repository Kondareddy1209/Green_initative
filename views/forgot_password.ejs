<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Forgot Password • MyGreenHome</title>
    <link rel="stylesheet" href="/styles/auth.css">
</head>
<body>
    <div class="auth-container">
        <h2>Forgot Your Password?</h2>
        <p style="text-align: center; margin-bottom: 20px; font-size: 14px; color: #555;">Enter your email to receive a password reset OTP.</p>

        <% if (error) { %>
            <p class="error-msg" style="text-align: center;"><%= error %></p>
        <% } %>
        <% if (message) { %>
            <p class="message-success" style="text-align: center; color: green;"><%= message %></p>
        <% } %>

        <form action="/auth/forgot-password" method="POST">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" required placeholder="your.email@example.com">
            </div>
            <div class="form-actions" style="justify-content: center;">
                <button type="submit">Send Reset OTP</button>
            </div>
            <p style="text-align: center; margin-top: 20px; font-size: 14px;">
                <a href="/auth" style="color: #4A90E2; text-decoration: none;">Back to Login</a>
            </p>
        </form>
    </div>
</body>
</html>